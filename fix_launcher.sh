perl -0777 -pi -e 's/    val createDocumentLauncher = androidx.activity.compose.rememberLauncherForActivityResult\(\n        androidx.activity.result.contract.ActivityResultContracts.CreateDocument\("application\/pdf"\)\n    \) \{ uri ->\n        if \(uri != null\) \{\n            isGenerating = true\n            generateQuestionPapersInternal\(context, exam, questions, uri, viewModel\) \{\n                isGenerating = false\n            \}\n        \}\n    \}/    val coroutineScope = rememberCoroutineScope()\n    val createDocumentLauncher = androidx.activity.compose.rememberLauncherForActivityResult(\n        androidx.activity.result.contract.ActivityResultContracts.CreateDocument("application\/pdf")\n    ) { uri ->\n        if (uri != null) {\n            if (pendingAction == "generatePapers") {\n                isGenerating = true\n                generateQuestionPapersInternal(context, exam, questions, uri, viewModel) {\n                    isGenerating = false\n                }\n            } else if (pendingAction == "downloadAnswerKey") {\n                isGenerating = true\n                coroutineScope.launch {\n                    generateAnswerKeyPdf(context, exam, uri, viewModel)\n                    isGenerating = false\n                }\n            }\n        }\n    }/g' app/src/main/java/com/example/ui/ExamDashboardScreen.kt
